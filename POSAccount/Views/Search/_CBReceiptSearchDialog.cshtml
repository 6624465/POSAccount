@using POSAccount.ViewModals.Search
@model POSAccount.ViewModals.Search.SearchResultVm<POSAccount.ViewModals.Search.CBReceipt>


<div class="modal-body">
    <div style="display:none;">@{ Html.RenderPartial("_SearchBoxControls", Model.searchWrapper); }</div>
    <div class="row">
        <div class="col-lg-12 col-xs-12">
            <div class="md-table">
                <table class="table table-hover table-striped table-bordered">
                    <thead>
                        <tr>
                            <th>Document No</th>
                            <th>Document Date</th>
                            <th>Debtor Code</th>
                        </tr>
                    </thead>
                    <tbody>
                        @for (var i = 0; i < Model.ListData.Count; i++)
                        {
                            <tr class="cursorCss" onclick="trClick('tr_@i','@Model.searchWrapper.searchVm.SearchTable')"
                                id="tr_@i" data-document-no="@Model.ListData[i].DocumentNo">
                                <td>@Model.ListData[i].DocumentNo</td>
                                <td>@Model.ListData[i].DocumentDate</td>
                                <td>@Model.ListData[i].DebtorCode</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>


